<!DOCTYPE html>
<html>
  <head>
    <%- include('./components/htmlHead'); %>
    <link rel='stylesheet' href='/css/login.css' />
  </head>
  <body>
    <div class="content">
      <div class="spacing"></div>
      <div id="loginForm">
        
        <form method="POST" action="/login" >
          <h3 id="title">Sign In</h3>
          <%if (error === true) { %>
            <p class="error_message">Incorrect username or password.</p>
          <% } %>

          <input type="hidden" name="redirect" value="<%= redirect %>">
          <input type="text" id="username" name="username" placeholder="Username" required>
          <input type="password" id="password" name="password" placeholder="Password" required>
          <button type="submit">Log in</button>
          
          <p>Don't have an account? <a href="/signup">Sign up</a> now!</p>
          <p style="color: olive;font-weight: bold;">Are you a PCS caretaker? <br>
            Enjoy a fast track sign up <a href="/ct_signup">here</a>!</p>
      </form>

      </div>
    </div>
  </body>
</html>
