<!DOCTYPE html>
<html>
    <head>
        <%- include('./components/htmlHead'); %>
        <link rel='stylesheet' href='/css/form.css' />
        <script src="js/leaveAvailabilityScript.js"></script>
    </head>

    <body>
        <header>
            <%- include('./components/header'); %>
        </header>

        <div class="spacing"></div>
        <div class="content">
          <form id="addAvailabilityForm" method="POST" action="/addAvailability">
            <h3 id="title">Add Availability</h1>
            <label for="day">Day: </label>
            <input type="text" id="day" name="day" placeholder="DD" required>
            <label for="month">Month: </label>
            <input type="text" id="month" name="month" placeholder="MM" required>
            <label for="year">Year: </label>
            <input type="text" id="year" name="year" placeholder="YYYY" required>
            <button type="submit" class="button">Submit</button>
          </form>
        </div>

        <script>
          window.addEventListener('load', function() {
            var forms = [document.getElementById('addAvailabilityForm')];
            var validation = Array.prototype.filter.call(forms, function(form) {
              form.addEventListener('submit', check, false);
            });
          }, false);
          
          if ("<%= msg%>" != "") {
              alert('<%= msg%>');
          }
        </script>

        <footer>
            <%- include('./components/footer'); %>
        </footer>
    </body>
</html>
